# 44-增量同步与Parquet缓存

- 目标：验证本地 Parquet 缓存、尾部补拉、去重合并和断点状态写回。
- 状态：pass

## 过程记录

- 首次同步写入 Jan 窗口缓存。
- 二次同步只补拉从最新缓存日开始的尾部，并用重叠日覆盖旧值。
- 三次同步完全命中缓存，再次加载时无需网络请求。

## 产物

- artifacts/cache/state.json

## 结论

- 增量同步、Parquet 缓存和断点状态链路可工作。
